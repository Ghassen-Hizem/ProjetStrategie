using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InstantiatePlayer : MonoBehaviour
{


    //a small panel on the left with images of units
    //Start button on top
    //nbr units selected / 20

    //at start enemyscripts and InputPlayer are desabled
    //we drag and drop from UI in the start zone and we update nbr units selected (we cant do that if selectedUnits ==20)
    //we can remove a player (destroy it and remove it from the number of selected)
    //if we click on start, the InstantiatePanel disappears, the gamePanel appears and the scripts are enabled 
    
}
